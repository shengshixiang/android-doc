# README

tmux使用

# 安装

* sudo apt-get install tmux

# 命令

* vim ~/.bashrc 

```
alias tn='tmux new -s'
alias tl='tmux ls'
alias ta='tmux attach -t'
alias tk='tmux kill-session -t'
```